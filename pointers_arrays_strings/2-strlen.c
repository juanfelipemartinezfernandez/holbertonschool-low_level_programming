#include "main.h"
/**
 * _strlen - function that returns the length of a string.
 * @s: string to analize
 * Return: it returns the lenght of the string
 */
int _strlen(char *s)
{
int contador = 0;

while (1)
{
if (*(s + contador) != "\\0")
contador++;
else
0;
}
}
