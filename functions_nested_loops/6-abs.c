#include "main.h"
/**
 * _abs - shows the absolute value of a number
 * @n: number to make absolute
 * Return: aboslute number
 */
int _abs(int n)
{
    int absolute;

    if (n >= 0)
    return (n);
    
    return ((absolute=n*-1));


}